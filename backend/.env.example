# 复制本文件为 backend/.env 后填写（勿把真实密钥提交到仓库）
#
# 服务
PORT=8787
# 可选：production 时 500 错误对外文案收敛；开发可省略
# NODE_ENV=development

# 智谱大模型（可选）。不配置则仅使用题库内置关键词规则，不调用外网 API。
# 以下三个变量任选其一即可，代码读取优先级：ZHIPU_API_KEY > ZHIPU_APIKEY > ZHIPU_KEY
ZHIPU_API_KEY=
